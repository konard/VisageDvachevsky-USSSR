version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./videos:/app/videos:ro
      - ./data:/app/data
    environment:
      - FLASK_ENV=production
      - PORT=5000
    restart: unless-stopped
